export interface ISeo {
	title:string,
	keywords:string,
	description:string,
	canonical:string
}

export let config = {
	home: {
		title: 'home - spm, build up your design',
		keywords: 'custom, framework, design, workflow, prototype, spm',
		description: 'spm, style package manager',
		canonical: 'https://www.spm-style.com'
	}
}